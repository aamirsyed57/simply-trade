{
  "permissions": {
    "allow": [
      "Bash(curl -s http://localhost:8000/openapi.json)",
      "Bash(python3 -c \"import json,sys; paths=json.load\\(sys.stdin\\)['paths']; [print\\(p\\) for p in paths if 'historical' in p or 'yfinance' in p]\")",
      "Bash(curl -s http://localhost:8000/api/v1/strategies)",
      "Bash(curl -s http://localhost:8000/api/v1/symbols)",
      "Bash(python3 -c \"import sys,json; [print\\(f\\\\\"{s['id']:3} {s['ticker']:10} {s['exchange']}\\\\\"\\) for s in json.load\\(sys.stdin\\)]\")",
      "Bash(curl -s \"http://localhost:8000/api/v1/historical?limit=5\")",
      "Bash(curl -s \"http://localhost:8000/api/v1/historical/1?limit=5\")"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f graphify-out/graph.json ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md for god nodes and community structure before searching raw files.\"}}' || true"
          }
        ]
      }
    ]
  }
}
